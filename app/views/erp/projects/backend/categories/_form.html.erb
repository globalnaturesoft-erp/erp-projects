<%= form_for([erp_projects, :backend, category]) do |f| %>    
    <%= erp_form_control("text", {
        name: 'category[name]',
        value: category.name,                   
        label: t('.name'),
        placeholder: t('.placeholder_name'),
        required: true,
        errors: category.errors.full_messages_for(:name)
    }) %>
    <%= erp_form_control("dataselect", {
        name: 'category[parent_id]',
        value: category.parent_id,
        text: category.parent_name,
        placeholder: t('.placeholder_parent_category'),
        label: t('.parent_category'),
        url: erp_projects.dataselect_backend_categories_path(format: 'json'),
        create: {
            url: erp_projects.new_backend_category_path,
            title: t('.create'),
            container_selector: '.new_category',
            input_selector: 'input[name="category[name]"]'
        }
    }) %>
    <%= erp_form_control("textarea", {
        name: 'category[description]',
        value: category.description,                   
        label: t('.description'),
        placeholder: t('.placeholder_description')
    }) %>
    <hr>
    <%= erp_component('button/save', {
        text: t('submit')
    }) %>
    <%= erp_component('button/cancel', {
        text: t('cancel'),
        href: erp_projects.backend_categories_path
    }) %>
<% end %>